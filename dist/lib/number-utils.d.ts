import { Big } from 'big.js';
export declare class NumberUtils {
    static isStepping(start: number | undefined, value: number, stepping: number): boolean;
    static isWholeNumber(value: Big): boolean;
}
